import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.loginFirebase',
  appName: 'form-login',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
